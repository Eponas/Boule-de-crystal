import 'package:flutter/material.dart';

class LoveCompatibilityPage extends StatelessWidget {
  const LoveCompatibilityPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Event Page'),
    );
  }
}